# Local dev: CPU-only transcription API
# Copy .env.example to .env and set TRANSCRIBE_API_KEY before running.
services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    # Optional: mount app for live code reload (override CMD)
    # volumes:
    #   - ./app:/app/app
    # command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
