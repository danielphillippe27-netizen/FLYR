# Frozen migration history

Do not add backend migrations here. This directory remains only as historical mobile context.

All new WolfGrid database migrations belong in `Wolfgrid-WEB/supabase/migrations`, which is rebuilt from scratch in CI.
